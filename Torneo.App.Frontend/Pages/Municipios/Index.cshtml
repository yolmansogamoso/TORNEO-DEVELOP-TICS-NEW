@page
@model Torneo.App.Frontend.Pages.Municipios.IndexModel
@{
}
<h1 class="titulo_pages">Municipio</h1>
<h2 class="titulo_tabla">Listado Municipios</h2>
<a class="btn_add" asp-page="./Create"><i class="fa-regular fa-square-plus"></i></a>
<div class="container_tabla">
    <table class="tabla">
        <thead class="thead_container">
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Nombre</th>
                <th scope="col">Acciones</th>
            </tr>
        </thead>
        <tbody class="tbody_container">
            @foreach (var municipio in Model.municipios)
            {
                <tr>
                    <th scope="row">@municipio.id</th>
                    <td>@municipio.Nombre</td>
                    <td>
                        <a class="btn_details" asp-page="./Details" asp-route-id="@municipio.id"><i class="fa-solid fa-info"></i></a>
                        <a class="btn_delate"><i class="fa-solid fa-trash-can"></i></a>
                        <a class="btn_edit"><i class="fa-regular fa-pen-to-square"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
