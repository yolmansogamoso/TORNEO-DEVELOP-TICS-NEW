@page
@model Torneo.App.Frontend.Pages.Jugadores.IndexModel
@{
}
<h1 class="titulo_tabla">Listado de Jugadores</h1>

<div class="container_tabla">
    <table class="tabla">
        <thead class="thead_container">
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Nombre</th>
                <th scope="col">Acciones</th>
            </tr>
        </thead>
        <tbody class="tbody_container">
            @foreach (var jugador in Model.jugadores)
            {
                <tr>
                    <th scope="row">@jugador.Id</th>
                    <td>@jugador.Nombre</td>
                    <td>
                        <a class="btn_details" asp-page="./Details" asp-route-id="@jugador.Id"><i class="fa-solid fa-circle-info"></i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
